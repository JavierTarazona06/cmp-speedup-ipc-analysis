# cmp-speedup-ipc-analysis
TD/TP5 (ES201) repository: performance study of CMP architectures using gem5 with test_omp (OpenMP). Thread-count and CPU-type (in-order/OOO) sweep, extraction of cycles, IPC, and speedup, including tables, plots, and discussion.
